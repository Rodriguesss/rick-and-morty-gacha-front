import styled from "styled-components";

const PortalWeaponsWrapper = styled.div`
  display:  flex;
  align-items: center;
  gap: 5px;
`

const Box = styled.div``

export { PortalWeaponsWrapper, Box }